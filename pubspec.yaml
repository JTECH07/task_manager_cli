name: task_manager_cli
description: A sample command-line CLI task management application in Dart.
version: 1.0.0
repository: https://github.com/JTECH07/task_manager_cli

environment:
  sdk: ^3.10.7

# Add regular dependencies here.
dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
